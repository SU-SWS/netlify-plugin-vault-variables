{
  "name": "netlify-plugin-vault-variables",
  "version": "2.0.1",
  "description": "Use vault as the place to store environment variables.",
  "main": "src/index.js",
  "bin": "src/script.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "lint": "NODE_ENV=test eslint \"./src/**/*.{js,ts}\"",
    "lint:fix": "NODE_ENV=test eslint \"./src/**/*.{js,ts}\" --fix"
  },
  "keywords": [
    "netlify",
    "vault",
    "environment",
    "plugin"
  ],
  "author": "Stanford Web Services",
  "license": "GPL-3.0-or-later",
  "dependencies": {
    "dotenv": "^16.0.3",
    "node-vault": "^0.10.0",
    "toml": "^3.0.0"
  },
  "devDependencies": {
    "eslint-config-airbnb": "^19.0.4",
    "eslint-config-prettier": "^5.1.0",
    "eslint-plugin-prettier": "^5.0.0"
  },
  "peerDependencies": {
    "@netlify/plugin-nextjs": ">=5.2",
    "next": ">=13.5.6"
  },
  "peerDependenciesMeta": {
    "@netlify/plugin-nextjs": {
      "optional": true
    },
    "next": {
      "optional": true
    }
  }
}
